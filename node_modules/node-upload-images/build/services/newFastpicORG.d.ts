/// <reference types="node" />
import { GenericImageUploadService } from './genericImageUploadService';
export declare class NewFastpicORG extends GenericImageUploadService {
    imageDirectLinkRx: RegExp;
    formFilename: string;
    constructor();
    uploadFromBinary(imageData: Buffer, filename: string): Promise<{
        directLink: string;
    }>;
}
