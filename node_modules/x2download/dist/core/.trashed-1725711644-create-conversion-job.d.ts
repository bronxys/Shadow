import { AxiosRequestConfig } from "axios";
import { Optional } from "../types";
export type CreateConvertJobParsedData = {
    status: string;
    jobId: Optional<string>;
    statusCode: number;
    fileUrl: Optional<string>;
};
export type CreateConvertJobOptions = {
    serverUrl: string;
    videoId: string;
    fileType: string;
    quality: string;
    fileName: string;
    token: string;
    tokenExpiresAt: number;
    waitUntilGetUrl?: boolean;
    requestOptions?: AxiosRequestConfig<FormData>;
    corsProxyUrl?: string;
};
declare function createConversionJob({ serverUrl, videoId, fileType, quality, fileName, token, tokenExpiresAt, waitUntilGetUrl, requestOptions, corsProxyUrl }: CreateConvertJobOptions): Promise<CreateConvertJobParsedData>;
export default createConversionJob;
