import { AxiosRequestConfig } from "axios";
export type GetVideoInfoOptions = {
    videoUrl: string;
    origin?: string;
    requestOptions?: AxiosRequestConfig<FormData>;
    corsProxyUrl?: string;
};
declare function getVideoInfo({ videoUrl, origin, requestOptions, corsProxyUrl }: GetVideoInfoOptions): Promise<ParsedVideoInfo>;
type Extra = {
    a: string;
    t: number;
    p: string;
};
export type Format = {
    fileExtension: string;
    quality: string;
    qualityKey: string;
    size: number;
};
export type ParsedVideoInfo = {
    id: string;
    title: string;
    fileName: string;
    token: string;
    tokenExpiresAt: number;
    message: string;
    formats: Format[];
    extra: Extra;
};
export default getVideoInfo;
