import { Format, ParsedVideoInfo } from "./core/get-video-info";
export type X2downloadOptions = {
    corsProxyUrl?: string;
};
export type GetFromFormatOptions = {
    info?: ParsedVideoInfo;
    format: Format;
};
declare class X2download {
    corsProxyUrl?: string;
    private info?;
    constructor({ corsProxyUrl }?: X2downloadOptions);
    getInfo(url: string): Promise<ParsedVideoInfo>;
    getFileUrl({ format, info }: GetFromFormatOptions): Promise<string>;
}
export default X2download;
